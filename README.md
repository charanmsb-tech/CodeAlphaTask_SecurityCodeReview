# Secure-Code-Review
Secure Login System built with Python, SQLite, and bcrypt. Implements user registration, login, password change, and deletion with strong password validation. Uses parameterized queries to prevent SQL injection and stores only hashed passwords, ensuring secure authentication practices.
